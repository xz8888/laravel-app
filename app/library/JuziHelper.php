<?php

class JuziHelper{

    /** gets the pretty dates **/
	public function getPrettyDate($days){
       if($days < 7)
          return $days.Lang::line('');
       else if($days > 7 && $days <= 30)
       	  return 
       if($days >= 30){
          
       }
	}
}